package fr.icom.info.m1.balleauprisonnier_mvn.models;

public enum Actions {
	MOVE_LEFT,			// Deplacement du joueur vers la gauche
	MOVE_RIGHT,			// Deplacement du joueur vers la droite
	TURN_LEFT,			// Rotation de la visée vers la gauche (celle du joueur)
	TURN_RIGHT,			// Rotation de la visée vers la droite (celle du joueur)
	SHOOT				// Lancement de la balle possédée
}
